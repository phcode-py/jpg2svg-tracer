flask>=3.0
numpy>=1.24
opencv-python>=4.7
flask
gunicorn
Pillow
# Optional: faster Gaussian smoothing and nearest-neighbour loss computation.
# Falls back to pure-numpy implementations when not installed.
# scipy>=1.10
